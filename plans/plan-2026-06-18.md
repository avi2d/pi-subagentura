# Plan — `ralplan-consensus` Workflow (2026-06-18)

**Idea:** "lets create workflow from this ../pi-ralplan using workflow"

**Status:** Consensus reached on **2026-06-18** after **3 iterations** of the Planner ↔ Architect ↔ Critic loop. Approved by Architect (iter 3, `**VERDICT: APPROVE**`) and Critic (iter 3, `**VERDICT: ACCEPT**`).

---

## Architecture Decision Record (ADR)

### Decision
Encode the RALPLAN consensus planning protocol (Planner → Architect → Critic, optional Analyst-spec) as a single workflow script named `ralplan-consensus`, persisted at user scope via the existing `save_workflow` tool (`src/workflow.ts:1389-1422`) and invokable from any Pi session via `workflow("ralplan-consensus", args)`. The script text lives in-repo at `src/workflows/ralplan-consensus.js` as the single source of truth; tests and runtime share the same string. The DELIBERATE signal table in the script pins to spec §8 verbatim (22 substring + 1 word-boundary = 23 entries) for this iteration.

### Drivers
1. **Spec completeness.** `plans/spec-2026-06-18.md` defines a fixed API surface (`RalplanArgs`/`RalplanResult` in §9), ten acceptance criteria (§11), and a non-trivial ordering invariant (Planner → Architect must be sequential, never parallel — §3 FR-3). The `workflow` primitive is the only existing tool in the codebase that can host this.
2. **Reuse over rewrite.** `src/workflow.ts:519-545` already provides `runWorkflow` with `agent/parallel/pipeline/phase/log/workflow/args/budget` injection, determinism guards, token accounting, and the saved-workflow registry. Building a parallel pipeline would duplicate all of this and split the test surface.
3. **Token economics.** Sequential phases + 5-iteration cap = at most 20 `agent()` calls (NFR-2), well under `MAX_TOTAL_AGENTS=1000` (`src/workflow.ts:49`). The in-process `startSubagentJob` backend (default per NFR-8) avoids the latency cost of tmux/zellij for a protocol that does not benefit from attachability.

### Alternatives Considered
- **A1: Build a new `ralplan` tool in `src/subagent.ts` next to the sub-agent tools.** Rejected: would split the orchestration surface; the `workflow` primitive is the right shape (script-as-data, deterministic vm sandbox, saved-workflow registry); duplicating the parsing/execution lifecycle is unjustified.
- **A2: Distribute the script via npm package `files` instead of user-scoped `save_workflow` (Option C in the planner draft).** Rejected: OQ-9 explicitly defers distribution; user-scope persistence matches the spec's R-9 mitigation; bundled scripts would bloat the published tarball for users who never invoke `ralplan-consensus`. Acknowledged tradeoff: bundled = version-locked reproducibility, user-scope = distribution flexibility; spec already chose flexibility at OQ-9.
- **A3: Use `isolation: "process"` (tmux/zellij) for one or more roles.** Rejected: NFR-8 + R-9 — the protocol gains nothing from attachability; in-process is faster and avoids the `isolation_process_fallback` debug-log noise on hosts without a multiplexer.
- **A4: Implement DELIBERATE mode pre-mortem and expanded test plan in this iteration.** Rejected: spec §13 OQ-7 tri-state plus the explicit `Mode: SHORT` here; DELIBERATE pre-mortem is opt-in. The script supports the tri-state (`args.deliberate: true | false | null`); it just doesn't emit pre-mortem/test-plan blocks unless `mode === "DELIBERATE"`.
- **A5: Match the 24-entry source (`prompts.ts:338-366`) verbatim instead of spec §8's 23 entries.** Rejected for v1: would silently change the spec §11 AC-3 test list. Pinning to spec §8 keeps the contract stable; the 24-vs-23 drift is logged as F-6 for a future iteration.

### Why Chosen
Option A (self-contained script + in-repo fixture) over Option B (script-only-in-test-fixture) for read-diff reviewability of the role prompts, which are the load-bearing part of the protocol. User-scoped `save_workflow` over npm-bundled for the same reason OQ-9 deferred it. Spec §8 over source `prompts.ts:338-366` as the canonical signal list — the spec is the contract for this iteration; drift is logged not papered over. The script mirrors the existing signal table byte-for-byte (the spec's FR-6) and reuses the existing verdict regex from spec §7. The implementation is a thin shim — `parseWorkflow` already validates the script; `runWorkflow` already provides the lifecycle; `saveWorkflowScript` already persists.

### Consequences

**Positive:**
- Single source of truth for the protocol (`src/workflows/ralplan-consensus.js`); one file to review on PRs.
- Tests exercise the *actual* script string (no parallel fixture that can drift).
- Reuses all existing `workflow` infrastructure — no new exports, no new tools, no new dependencies.
- The 11 spec ACs map 1:1 to test cases; coverage is provable from `git grep` of the test file.
- Banned-token static check is now a vitest test (T2 + T5), not a manual grep.

**Negative:**
- The script duplicates role-prompt text from `pi-ralplan/pi/skills/ralplan/prompts/*.md` (R-1). Mitigation: header comment + resync procedure in `CONTRIBUTING.md` (T4).
- The Critic agent's file write is the only way `plans/<planName>.md` exists; the script cannot verify the write happened (NFR-1 forbids `fs` in-sandbox). Mitigation: the `PLAN_WRITTEN:` marker grep (R-8).
- The `vm` sandbox is not a security boundary (`src/workflow.ts:20-23`); an adversarial author could escape. Acceptable because the author is the trusted main agent (per `src/workflow.ts:20`).
- DELIBERATE auto-detection inlines the 22-substring signal table; if `pi-ralplan` adds a signal, the script must be re-synced manually (no live import — that would require `fs` in the sandbox). The vitest `deliberateSignals.length === 22` drift fence catches silent drift in CI.
- User-scope persistence (`save_workflow`) means the saved script does not auto-resync with `pi-ralplan` releases; F-6 (mechanical diff tool) is the mitigation and is out of scope for v1.
- **Spec-vs-source signal drift (23 vs 24 entries)** is now an explicit known issue rather than a phantom 19-entry claim. Acknowledged in F-6.

### Follow-ups
- **F-1.** When `pi-ralplan` adds or removes a DELIBERATE signal, update both `src/workflows/ralplan-consensus.js` and the AC-3 test list in `src/workflow-ralplan.test.ts`. The vitest `deliberateSignals.length === 22` fence catches silent drift in CI.
- **F-2.** If the `workflow` tool ever gains structured output capture (currently `runWorkflow` returns only the script's `return` value plus counters), expose a "machine-readable consensus summary" as a follow-up workflow — OQ-10 deferred.
- **F-3.** Add a `npm run sync:ralplan` script that diffs the four role-prompt files against the inlined constants and exits non-zero on drift. Out of scope for this iteration. **(Promoted from "out of scope" by Architect's synthesis — see ADR Alternatives A5 / Consensus Addendum.)**
- **F-4.** Track the `RALPLAN-DR Summary` block in the *plan_draft.md* for the next iteration's RALPLAN review: the "Viable Options" section considers A/B/C but did not need to enumerate pre-mortem scenarios because mode is SHORT.
- **F-5.** If acceptance of this plan is reached, the next step is `node -e ... saveWorkflow("ralplan-consensus", ...)` from a parent agent — not automated in this iteration, per OQ-9.
- **F-6.** Reconcile the 23-entry spec §8 list with the 24-entry source `pi-ralplan/pi/extensions/ralplan/prompts.ts:338-366` (which includes `"remove"`). Either (a) amend spec §8 to add `"remove"` and update the AC-3 subtest list, or (b) amend `prompts.ts` to drop `"remove"`. Tracked here so it doesn't get lost — out of scope for v1.

---

## Planning Flow

### Open Questions
- [x] **OQ-1** Analyst phase: include/skip/conditional? — **Resolved** (conditional on `args.specPath` absence)
- [x] **OQ-2** Role prompt source — **Resolved** (inline as string literals)
- [x] **OQ-3** Who writes `plans/plan.md`? — **Resolved** (Critic on ACCEPT; script confirms via `PLAN_WRITTEN:` marker)
- [x] **OQ-4** `ACCEPT-WITH-RESERVATIONS` semantics — **Resolved** (counts as success; reservations surfaced in feedback)
- [x] **OQ-5** Escalation on disagreement — **Resolved** (throw structured error; caller decides)
- [x] **OQ-6** Default `workingDir` — **Resolved** (required; throws if absent or non-absolute)
- [x] **OQ-7** DELIBERATE tri-state — **Resolved** (`true`→DELIBERATE, `false`→SHORT, omitted→auto-detect)
- [x] **OQ-8** Workflow composition/nesting — **Resolved** (keep flat)
- [x] **OQ-9** Distribution path — **Resolved** (user-scoped via `save_workflow`; deferred bundling)
- [x] **OQ-10** Machine-readable consensus summary — **Resolved** (deferred to v2 / F-2)

### Plan Iterations
- **Iteration 1**: Initial draft (264 lines)
  - Planner emitted the initial RALPLAN-DR Summary, 5-task plan (T1–T5), ADR section
  - Status: **REVISION NEEDED** by Architect (see iteration-1 review)
- **Iteration 2**: Addressed iteration-1 CRITICAL/MAJORs (signal count fix, AC-5 structured Error, AC-11 for empty `args.idea`, `extractFeedbackSection` semantics pinned, MINORs)
  - Status: **REVISION NEEDED** by Architect (2 NEW MAJORs introduced by the fixes themselves: off-by-one in `iterations` counter, feedback length mismatch)
- **Iteration 3**: Addressed iteration-2 MAJORs (renamed loop counter `iter` → `iterations` 1-based; added `iterNum < maxIterations` guard around `feedback.push`)
  - Status: **APPROVE** by Architect → **ACCEPT** by Critic → **consensus reached**

### Tradeoffs Discussed
- **Distribution: user-scoped save vs. npm-bundled asset (Options A/B/C)**
  - Options considered:
    - A: Self-contained script + in-repo fixture (RECOMMENDED)
    - B: Script lives only inside a test fixture string
    - C: Bundle the script into the npm package as a static asset
  - Rejected alternatives:
    - ✗ Option B — read-diff reviewability suffers; drift becomes quieter (silent script diff)
    - ✗ Option C — bloats tarball; spec OQ-9 deferred this; role-prompt drift becomes version-locked to the wrong package
  - Decision: Option A. Real tradeoff is **reproducibility (C) vs. distribution flexibility (A/B)**. Spec already chose flexibility.

- **DELIBERATE signal list source of truth**
  - Options considered:
    - Spec §8 (23 entries: 22 substring + 1 word-boundary)
    - Source `pi-ralplan/pi/extensions/ralplan/prompts.ts:338-366` (24 entries)
  - Rejected alternative: ✗ Source file (would silently change AC-3 test list; spec is the contract for this iteration)
  - Decision: Pin to spec §8; drift logged as F-6 (reconcile in a future iteration).

- **Loop counter semantics**
  - Options considered:
    - 0-based, increment at end of loop
    - 1-based, count completed iterations (matches AC-4 = 1 on first-iter dual-approve)
  - Rejected alternative: ✗ 0-based (caused off-by-one vs AC-4)
  - Decision: 1-based; `iterations = iterNum; break;` on dual-approve.

- **Feedback push policy**
  - Options considered:
    - (a) Skip push for the final iteration (no next Planner to consume)
    - (b) Push unconditionally; AC-5 expects `maxIterations` entries
  - Rejected alternative: ✗ (b) — generates stale feedback that no consumer will read
  - Decision: Option (a). Rationale: "feedback for iteration N feeds iteration N+1."

### Architect Reviews
- **Iteration 1 review** [REVISION NEEDED]
  - Feedback: Signal-count contradiction (claimed 19 entries, spec has 23, source has 24); AC-5 missing `.draftPath`/`.feedback` on thrown error; no AC for IR-8 (empty `args.idea`); `extractFeedbackSection` cap semantics ambiguous; 5 MINORs (meta.phases reference, "header fence markers" terminology, no absolute-path validation, banned-token CI check missing, log ordering).
  - Steelman surfaced: Option B's drift argument cuts both ways; F-3 (sync script) promoted from out-of-scope.
- **Iteration 2 review** [REVISION NEEDED]
  - Feedback: Iteration-1 CRITICAL/MAJORs addressed. NEW MAJORs introduced: off-by-one in AC-4 (script returns `iterations: 0` after first-iter dual-approve); feedback length mismatch in AC-5 (unconditional push yields 5 entries for maxIterations=5, but AC-5 expects 4).
- **Iteration 3 review** [APPROVE]
  - Feedback: Both iteration-2 MAJORs mechanically fixed. Hand-trace of AC-4 and AC-5 verified. No new regressions; rename `iter` → `iterations` is clean. 3 pre-existing MINORs carried forward (spec §9 vs plan feedback shape, AC-6 sub-case ordering, analystPrompt template not drafted) — none blocking.

### Critic Reviews
- **Iteration 3 review** [ACCEPT]
  - Feedback: All `src/workflow.ts` line refs verified (442, 248-282, 519-545, 798, 1087, 1389-1422, 166-179, 453-503). Signal count 23 matches spec §8; 24-entry source drift documented in F-6. 6 MINORs (none blocking) + 1 amber gap in `extractFeedbackSection` truncation policy.
  - Ralplan gate summary row:
    - Principle/Option Consistency: **Pass** — Principles P1–P5 align 1:1 with viable Options A/B/C and the final recommendation.
    - Alternatives Depth: **Pass** — Option B steelman strengthened; Option C added with explicit invalidation rationale.
    - Risk/Verification Rigor: **Pass** — R-1..R-14 cover role-prompt drift, vm sandbox, token exhaustion, agent failures, spec-vs-source drift; each tied to a vitest test or T-step.
    - Deliberate Additions (if required): **N/A** — mode is SHORT.

### Decisions
- **Decision: Workflow encoding** — Use the existing `workflow` primitive (`src/workflow.ts:519-545`) with a single self-contained script string. [status: approved]
- **Decision: Distribution path** — User-scoped `save_workflow`, not npm-bundled. [status: approved]
- **Decision: Role prompt source** — Inline as string literals in `src/workflows/ralplan-consensus.js`. [status: approved]
- **Decision: Analyst persona** — Synthesized (no source file in pi-ralplan); conditional on `args.specPath` absence. [status: approved]
- **Decision: DELIBERATE mode default** — Auto-detect from `args.idea`; pin to spec §8 (23 entries). [status: approved]
- **Decision: ACCEPT-WITH-RESERVATIONS** — Counts as consensus success; reservations surfaced in `feedback[]`. [status: approved]
- **Decision: Loop counter semantics** — 1-based; matches AC-4 (`iterations === 1` on first-iter dual-approve). [status: approved]
- **Decision: Feedback push policy** — Skip push for final iteration (no next consumer); matches AC-5 (`feedback.length === 4` for maxIterations=5). [status: approved]
- **Decision: Final plan writer** — Critic agent on ACCEPT verdict; script confirms via `PLAN_WRITTEN:` marker grep. [status: approved]
- **Decision: `PIPELINE_RALPLAN_COMPLETE` log ordering** — Logged before `return` so it surfaces in the progress stream. [status: approved]

### Approvals
- ✓ **Iteration 1 draft** by Planner — approved-as-starting-point
- ✓ **Iteration 2 draft** by Planner — revision of iteration 1
- ✓ **Iteration 3 draft** by Planner — final
- ✓ **Iteration 3 review** by Architect — APPROVE
- ✓ **Iteration 3 review** by Critic — ACCEPT

### Rejections
- ✗ **Iteration 1 review** by Architect — REVISION NEEDED (signal count, AC-5 fields, IR-8, cap semantics)
- ✗ **Iteration 2 review** by Architect — REVISION NEEDED (off-by-one, feedback length)
- ✗ **Distribution via npm bundle** — rejected (OQ-9 deferred)
- ✗ **`isolation: "process"` for any role** — rejected (NFR-8)
- ✗ **DELIBERATE pre-mortem/test plan in v1** — rejected (mode is SHORT; opt-in)
- ✗ **24-entry source verbatim** — rejected (would silently change AC-3; pinned to spec §8 instead)

---

## Task Breakdown (file-path index)

| Task | File | Status |
|------|------|--------|
| T1   | `src/workflows/ralplan-consensus.js` (create) | pending |
| T2a  | `src/workflow-ralplan.test.ts` (create) | pending |
| T2b  | `src/workflow-ralplan-banned-tokens.test.ts` (create) | pending |
| T3   | `src/workflow-ralplan.test.ts` (extend, same file as T2a) | pending |
| T4a  | `src/workflows/ralplan-consensus.js` (extend header) | pending |
| T4b  | `CONTRIBUTING.md` (append resync subsection) | pending |
| T5   | none (CI verification) | pending |

### T1 — Create the workflow script text at `src/workflows/ralplan-consensus.js`

**Files:**
- Create: `src/workflows/ralplan-consensus.js`
- Touched (existing): `package.json` `files` array — **deliberately not** extended (OQ-9 user-scoped)

**Script body contents:**
1. `export const meta = { name: "ralplan-consensus", description: "...", phases: [...] }` — must be a pure literal or `parseWorkflow()` throws (`src/workflow.ts:166-179`).
2. `ARCH_VERDICT_RE` and `CRIT_VERDICT_RE` constants per spec §7.
3. `DELIBERATE_SIGNALS` (exactly **22 entries** matching spec §8 verbatim) + `WORD_BOUNDARY_SIGNALS = new Set(["rm"])` + `isDeliberate(idea)`. Bare `"auth"` intentionally absent.
4. `extractFeedbackSection(text, pattern, cap = 2000)` — per-reviewer ≤ 2000, per-iteration total ≤ 4000 (Architect 2000 + Critic 2000).
5. Inline `PLANNER_PERSONA`, `ARCHITECT_PERSONA`, `CRITIC_PERSONA`, `ANALYST_PERSONA` template literals — copy of `pi-ralplan/pi/skills/ralplan/prompts/{planner,architect,critic}.md`. Header comment records source paths + last-synced date.
6. `args` validation BEFORE any `agent()` call: `args.idea` non-empty, `args.workingDir` absolute, `args.specPath` string-or-undefined.
7. Sequential loop body: `iterations` is 1-based; dual-approve sets `iterations = iterNum` then breaks; `if (iterNum < maxIterations)` guard around `feedback.push`; error exposes `.verdicts/.draftPath/.iterations/.feedback/.mode`; `log("PIPELINE_RALPLAN_COMPLETE")` BEFORE `return`.
8. `mode` computed once (true/false/auto-detect) and stays sticky across iterations (IR-6).

**Acceptance criteria (T1):**
- [ ] Script contains `export const meta`
- [ ] Zero matches of banned tokens: `Date.now(`, `Math.random(`, `new Date(`, `require(`, `process.`, `fs.`, `path.`
- [ ] `deliberateSignals.length === 22` and `WORD_BOUNDARY_SIGNALS.has("rm") === true`
- [ ] `ARCH_VERDICT_RE` and `CRIT_VERDICT_RE` byte-equal spec §7
- [ ] Script body ≤ 1500 lines

### T2 — Unit tests

**Files:**
- Create: `src/workflow-ralplan.test.ts`
- Create: `src/workflow-ralplan-banned-tokens.test.ts`

**Test cases (each maps to one AC in spec §11):**

| AC | Test name |
|----|-----------|
| AC-1 | saves ralplan-consensus via `saveWorkflowScript` and lists it |
| AC-2 | non-deliberate idea → SHORT mode |
| AC-3 | deliberate signals: `migration`/`authentication`/`PII`/`schema`/`production`/`breaking change` → DELIBERATE |
| AC-3.1 | bare `auth` in "Add an author bio field" does NOT trigger DELIBERATE |
| AC-4 | mocked dual-approve exits at iteration 1 with expected shape |
| AC-5 | perpetual-reject exits at maxIterations with structured error exposing `.verdicts/.draftPath/.iterations/.feedback` |
| AC-6 | verdict regex: canonical + whitespace + case + UNPARSED fallback |
| AC-7 | missing/non-absolute `args.workingDir` throws before any `agent()` call |
| AC-8 | Planner returning null throws "Planner returned null at iteration N" |
| AC-9 | `maxIterations` default is 5 |
| AC-10 | saved workflow invokable by name via `workflow()` composition |
| AC-11 | empty/whitespace `args.idea` throws before any `agent()` call |

Plus `extractFeedbackSection` tests: cap, order preservation, per-iteration total ≤ 4000.

**Acceptance criteria (T2):**
- [ ] All AC-1..AC-11 covered (≥ 14 `it()` blocks across the two files)
- [ ] No test depends on real LLM calls (mocked `runAgent` only)
- [ ] Tests run in < 2 s wall clock
- [ ] `deliberateSignals.length === 22` asserted directly (drift fence)
- [ ] Banned-token test reads the script text and asserts zero matches

### T3 — `save_workflow` + `list_workflows` round-trip

**Files:** `src/workflow.ts` — **no edits expected**. Test extensions in `src/workflow-ralplan.test.ts`.

Three cases:
1. `saveWorkflowScript` then `loadWorkflowScript` returns byte-identical text
2. `listSavedWorkflows` after save includes the entry
3. `sanitizeWorkflowName("ralplan-consensus")` does not throw

### T4 — Documentation

**Files:**
- Touched: `src/workflows/ralplan-consensus.js` (first 30 lines = comment block listing 4 source paths + `last-synced:`)
- Touched: `CONTRIBUTING.md` (append "Resync the `ralplan-consensus` workflow" subsection ≤ 15 lines)

### T5 — CI verification

**Steps from repo root:**
1. `npm run typecheck` — must pass
2. `npm test` — must pass (T2 + T2b run as part of the suite)
3. `npm run format:check` — must pass
4. `npm run pack:check` — must pass (script NOT in `package.json#files` per OQ-9)

---

## Dependency Graph

```
T1 ─┬─► T2a ─┬─► T5 (CI gate)
    ├─► T2b ─┘
    ├─► T3  ─┘
    └─► T4a ─► T4b ─► T5
```

T1 is the only strict predecessor. T2a/T2b/T3/T4 are independent and may be developed in parallel. T5 is the merge gate and runs all four checks.

---

## Acceptance Criteria per Task

Inline above; cross-referenced from the AC table in T2. Total ACs covered: **AC-1..AC-11**.

---

## Risk Register

| ID | Risk | Mitigation |
|----|------|------------|
| R-1 | Role prompt drift | T4 header + CONTRIBUTING resync procedure |
| R-2 | Verdict parsing fragility | AC-6 test case covers whitespace + case + UNPARSED |
| R-3 | Token exhaustion | T1 loop calls `extractFeedbackSection(text, pattern, 2000)`; per-iteration total ≤ 4000 chars; ACs in T2 |
| R-4 | Agent `null` returns | AC-8 throws with phase + iteration |
| R-5 | vm sandbox escape | T1 acceptance: zero references to banned tokens — automated in T2b + T5 |
| R-6 | Missing `args.specPath` on disk | Caller (parent agent) owns the disk-existence check; the script cannot check it (fs banned in vm sandbox). T1 args validation only checks type/emptiness. If caller passes a non-existent path, the Analyst agent fails to read it and the workflow surfaces that failure via the agent-null error path. |
| R-7 | `MAX_TOTAL_AGENTS` cap | 20 max agents; well under cap; T2 default-concurrency test |
| R-8 | Critic fails to write final plan | T1 loop greps for `PLAN_WRITTEN:` marker; surfaced in T2 error-shape test |
| R-9 | Distribution path | OQ-9: user-scoped; `package.json#files` deliberately not extended; T5 verifies |
| R-10 | Machine-readable summary | OQ-10: out of scope for v1; F-2 in follow-ups |
| R-11 | Spec §8 vs source drift (23 vs 24 signals) | Pin to spec §8 for v1; vitest length fence catches silent drift; F-6 reconciliation logged |
| R-12 | Non-absolute `workingDir` | T1 args validation rejects with explicit error; AC-7 sub-case tests it |
| R-13 | Empty `args.idea` (IR-8) | T1 args validation rejects before any `agent()` call; AC-11 covers it |
| R-14 | `PIPELINE_RALPLAN_COMPLETE` lost after return | T1 step 7 explicitly logs BEFORE `return` |

---

## Artifacts

| File | Purpose |
|------|---------|
| `plans/spec-2026-06-18.md` | Combined requirements + technical spec |
| `plans/open-questions.md` | Persisted Open Questions (all resolved) |
| `plans/drafts/plan_draft.md` | Planner's iteration-3 draft (348 lines) |
| `plans/drafts/architect_review.md` | Architect's iteration-3 review (APPROVE) |
| `plans/drafts/critic_review.md` | Critic's iteration-3 review (ACCEPT) |
| `plans/plan-2026-06-18.md` | **This file** — consensus-approved final plan |
